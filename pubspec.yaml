name: online_book_review_app
description: Online Book Store App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^0.13.4
  syncfusion_flutter_pdfviewer:
  webview_flutter: ^2.1.2
  cached_network_image:
  flutter_rating_stars: ^1.0.3+4
  firebase_core: ^1.10.0
  firebase_auth: ^3.2.0
  provider: ^6.0.1
  cloud_firestore:
  loader_overlay:
  infinite_scroll_pagination: ^3.1.0
  flutter_staggered_animations:
  url_launcher:

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
